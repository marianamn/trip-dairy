<section>
    </div>
        {{#each this}}
            <div>
                <a href="#/trip-diaries/{{_id}}" data-id="{{_id}}">
                    <p><img src="{{this.mainImage}}" alt="main-image" style="width: 550px; height: 400px;"></p>
                    <p>{{this.likes}}</p>
                </a>
            </div>
        {{/each}}
    </div>
    
    <div id="most-recent"><div>
</section>

<aside>
    </div><div id="categories"></div>   
</aside>

