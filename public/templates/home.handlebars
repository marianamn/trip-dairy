<section>
    <div>Trips</div>
         {{#each data}}
         <div>
            <p>{{author}}</p>
            <p>{{category}}</p>
            <p>{{content}}</p>
            <p>
                 {{#each images}}
                 <img src="{{this}}" alt="">
                 {{/each}}
            </p>
            <p>{{place}}</p>
            <p>{{title}}</p>
            <p>{{likes}}</p>
            <p>{{dislike}}</p>
            </div>
         {{/each}}
    </div>
</section>